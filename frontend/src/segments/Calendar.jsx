import { Box } from "@chakra-ui/react";
import Structure from "./SegmentStructure";

export default function({ setMobileDrawerOpen }) {
    return (
        <Structure title={'Kalender'} setMobileDrawerOpen={setMobileDrawerOpen}>
            <Box w={'100%'} h={'100%'}>
                
            </Box>
        </Structure>
    );
}